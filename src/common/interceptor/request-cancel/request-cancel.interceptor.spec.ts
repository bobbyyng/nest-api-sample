import { RequestCancelInterceptor } from './request-cancel.interceptor';

describe('RequestCancelInterceptor', () => {
  it('should be defined', () => {
    expect(new RequestCancelInterceptor()).toBeDefined();
  });
});
